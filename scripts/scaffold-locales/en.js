/** English defaults — no overrides (template content is already English). */
export default {
  contact: {
    emailLabel: 'Email this brief',
    whatsappLabel: 'WhatsApp this brief',
    subjectPrefix: 'Interest in'
  }
};
